# Task 4.2.1: Update Protobuf for gRPC Callback Service

## Objective
Define a standard gRPC service that clients must implement to receive callbacks via the gRPC protocol.

## Requirements
- Add a `BoomerangCallback` service to `boomerang.proto`.
- Define a `CallbackRequest` message containing the task ID and binary payload.
- Define a `CallbackResponse` message for acknowledgment.
- Ensure the generated Java code is updated.

## Proposed Changes
### `boomerang-proto/src/main/proto/boomerang.proto`
- Define `service BoomerangCallback`.
- Define `rpc OnTaskExpired(CallbackRequest) returns (CallbackResponse)`.

## Verification Plan
- Run `./gradlew :boomerang-proto:generateProto` to ensure the schema is valid and code is generated.
- Verify the generated `BoomerangCallbackGrpc` class exists.
