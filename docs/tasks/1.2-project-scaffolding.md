# Task 1.2: Project Scaffolding

## Status: Complete ✅

## Objective
Set up the core Java project infrastructure using Gradle (Kotlin DSL), integrating Netty, RocksDB, and Protobuf.

## Implementation Details
- **Build System:** Gradle multi-module setup (`boomerang-proto`, `boomerang-core`, `boomerang-cli`).
- **Java 21:** Configured via Gradle toolchain.
- **Protobuf:** Configured `boomerang-proto` with the `com.google.protobuf` plugin and moved `boomerang.proto` to `src/main/proto`.
- **Core Dependencies:**
    - Netty (Networking).
    - RocksDB (Persistence).
    - SLF4J/Logback (Logging).
- **CLI:** Configured `boomerang-cli` with `picocli` and GraalVM Native Image plugin.
- **Code Quality:**
    - **Spotless:** Configured with Google Java Format.
    - **JaCoCo:** Configured for test coverage reporting.
    - **JUnit 5:** Set up as the default test runner.

## Dependencies
- [x] Task 1.1: Define Protobuf Schema.

## Scope
- Initialize the build system (Maven/Gradle).
- Configure the Java 21 toolchain.
- Add core dependencies:
    - **Networking:** Netty (Protobuf support).
    - **Persistence:** RocksDB (Java wrapper).
    - **Protobuf:** Protobuf-java runtime and code generation plugin.
- Configure code quality tools (as per `GUIDELINES.md`):
    - **Spotless:** For Java formatting.
    - **Checkstyle:** For code standards.
    - **JaCoCo:** For test coverage (Target: 90%+).
    - **PITest:** For mutation testing.
- Establish the standard directory structure (`src/main/java`, `src/test/java`, `proto/`).
- Create a simple "smoke test" to verify the build and test pipeline.

## Decisions Made
- **Java Version:** Java 21 (LTS).
- **Build Tool:** Gradle (Kotlin DSL).
- **Project Metadata:**
    - **Group ID:** `io.boomerang`
- **Modularity:** Multi-module structure:
    - `boomerang-proto`: Protobuf generation.
    - `boomerang-core`: Main server logic (HTW, Persistence, Netty).
    - `boomerang-cli`: `boomtool` (GraalVM native).
- **Persistence/Netty:** Standardized on `netty-all` and `rocksdbjni`.

## Open Questions
- None.

## Dependencies
- [x] Task 1.1: Define Protobuf Schema.
